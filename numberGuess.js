// number Guess.js
const ps = require("promp sync");
const prompt = ps();

// Generate a ramdom number between 1 to 10
const Number = Math.floor(Math.random() * 10) + 1;
// guess  to 0
let guess = 0;
// Loop until the user's guess matches the number while (guess!== Number) {
// Prompt the user for input and convert it to guess = parInt(prompt("Guess a number between 1 to 10"));}

if (guess < randomNumber); {
    console.log("Too low!"); }
 if (guess > randomNumber) {
     console.log("Too high");
}

    